name: FibEdge Auto Refresh V3 (retired)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "15-minute automatic schedule retired. Use FibEdge Website Scan."
